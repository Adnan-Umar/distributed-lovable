spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: ${GITHUB_URI}
          username: ${GITHUB_USERNAME}
          password: ${GITHUB_PASSWORD}
          default-level: main

server:
  port: 8888
